;; ACP ast - see structure
;; in the ../struct.ast

(load "./defines.ss")

(define (parse tokens)
  (display "Go fuck yourself
           :) without negativity
  I'm just awk of writing
  parser in scheme\n"))
